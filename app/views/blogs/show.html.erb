<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @blog.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @blog.content %>
  <br>
  <div>
    <div>
      <P>更新日時</p>
      <%= @blog.updated_at %>
    </div>
    <div>
      <P>作成日時</p>
      <%= @blog.created_at %>
    </div>
  </div>
</p>

<%= link_to 'Edit', edit_blog_path(@blog) %> |
<%= link_to 'Back', blogs_path %>
